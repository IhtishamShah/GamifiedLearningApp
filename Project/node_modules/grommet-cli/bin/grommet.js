#!/usr/bin/env node
'use strict';

var _vorpal = require('vorpal');

var _vorpal2 = _interopRequireDefault(_vorpal);

var _version = require('./commands/version');

var _version2 = _interopRequireDefault(_version);

var _new = require('./commands/new');

var _new2 = _interopRequireDefault(_new);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
* Local dependencies
**/
var cli = (0, _vorpal2.default)();

/**
* NPM dependencies
**/

cli.use(_version2.default);
cli.use(_new2.default);

if (process.argv.length === 2) {
  // this means that only "grommet" has been typed
  // we should launch the CLI in this case
  cli.delimiter(cli.chalk.magenta('grommet~$')).show();
} else {
  // this means that more than "grommet" has been typed
  // we should execute the command instead
  cli.parse(process.argv);
}